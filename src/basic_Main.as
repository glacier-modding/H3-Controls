package
{
	import basic.BitmapReplacementOpenVR;
	import basic.BootFlowBackdropImage;
	import basic.BootFlowColorFill;
	import basic.BoundsExtender;
	import basic.Box;
	import basic.ButtonPrompt;
	import basic.ButtonPromptContainer;
	import basic.ButtonPromptImage;
	import basic.ButtonPromtUtil;
	import basic.Checkbox;
	import basic.DottedLine;
	import basic.GameChatIndicator;
	import basic.GlobalLoadindicator;
	import basic.Gradient;
	import basic.GradientFlipped;
	import basic.IButtonPromptOwner;
	import basic.ImageBox;
	import basic.ImageBoxSpatial;
	import basic.ImageButton;
	import basic.InfoBoxWithBackground;
	import basic.ItemBg;
	import basic.Line;
	import basic.LoadingScreenImageLoader;
	import basic.LoadingScreenObjectiveTile;
	import basic.LoadingTips;
	import basic.Mask;
	import basic.MaskCentered;
	import basic.MaskCircular;
	import basic.PieChart;
	import basic.Quad;
	import basic.SafeAreaScaleContainer;
	import basic.ScrollingList;
	import basic.Slider;
	import basic.SolidLine;
	import basic.Subtitle;
	import basic.TextBox;
	import basic.VideoBox;
	import basic.VideoBoxInfo;
	import flash.display.Sprite;
	
	public class basic_Main extends Sprite
	{
		
		public function basic_Main()
		{
			super();
			var _loc1_:Array = [BitmapReplacementOpenVR, BootFlowBackdropImage, BootFlowColorFill, BoundsExtender, Box, ButtonPrompt, ButtonPromptContainer, ButtonPromptImage, ButtonPromtUtil, Checkbox, DottedLine, GameChatIndicator, GlobalLoadindicator, Gradient, GradientFlipped, IButtonPromptOwner, ImageBox, ImageBoxSpatial, ImageButton, InfoBoxWithBackground, ItemBg, Line, LoadingScreenImageLoader, LoadingScreenObjectiveTile, LoadingTips, Mask, MaskCentered, MaskCircular, PieChart, Quad, SafeAreaScaleContainer, ScrollingList, Slider, SolidLine, Subtitle, TextBox, VideoBox, VideoBoxInfo];
		}
	}
}
